package util

const JOB_TYPE_EVERY_SECOND = "* * * ? * *"
const JOB_TYPE_EVERY_MINUTE = "0 * * ? * *"
const JOB_TYPE_EVERY_HOUR = "0 0 * ? * *"
const JOB_TYPE_EVERY_12AM = "0 0 0 * * ?"
const JOB_TYPE_EVERY_1AM = "0 0 1 * * ?"
